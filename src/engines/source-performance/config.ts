// config.ts
// File: src/engines/source-performance/config.ts
// Purpose: Static configuration for the Source Performance Engine (Engine 9).

/** Below this many `sent` events a source's rates are unstable → low_confidence. */
export const LOW_CONFIDENCE_SENT_THRESHOLD = 10;
